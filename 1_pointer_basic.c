#include<stdio.h>

int main() {
  //declare a variable a of type int and assing value 10 to it.
  int n = 50;
  //delacre a pointer variable p of the type int * and assign the 
  int *p = &n;

  
  *p = 39;
  printf("%p\n",p);

  float b = 34.54;
  //declare a pointer variable that would point to b.
  
}
